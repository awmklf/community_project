<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="testDAO">

<select id="testDAO.select" resultType="community.springmvc.service.TestVO">
	SELECT * FROM TEST
</select>

<insert id="testDAO.insert">
	INSERT INTO TEST VALUES (#{testVal})
</insert>

</mapper>